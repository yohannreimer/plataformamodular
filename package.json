{
  "name": "orquestrador-jornadas",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/backend",
    "apps/frontend"
  ],
  "scripts": {
    "dev:backend": "npm --workspace apps/backend run dev",
    "dev:frontend": "npm --workspace apps/frontend run dev",
    "demo:prymeira": "APP_DB_PATH=data/demo.db SEED_FINANCE_DEMO=true LOCAL_AUTH_BYPASS=1 VITE_LOCAL_AUTH_BYPASS=1 VITE_API_BASE_URL=http://localhost:4000 VITE_PRYMEIRA_HUB_URL=http://localhost:5175 npm run dev:backend & APP_DB_PATH=data/demo.db SEED_FINANCE_DEMO=true LOCAL_AUTH_BYPASS=1 VITE_LOCAL_AUTH_BYPASS=1 VITE_API_BASE_URL=http://localhost:4000 VITE_PRYMEIRA_HUB_URL=http://localhost:5175 npm run dev:frontend",
    "demo:reset": "mkdir -p ./apps/backend/data && rm -f ./apps/backend/data/demo.db",
    "build": "npm run build -w apps/backend && npm run build -w apps/frontend"
  }
}
